# Dictionaries

my_d = dict(
    name= "Mark",
    lastname= "McDonald" 
)

my_d2 = dict.fromkeys(["name", "lastname"]) # It reates a dictionary and fill all the members with None values

my_d3 = dict.fromkeys(["name", "lastname"], "default value") # It reates a dictionary and fill all the members with String "default value" values

